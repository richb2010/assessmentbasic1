const readline = require('readline')
const reader = readline.createInterface({
  input: process.stdin,
  output: process.stdout
})

reader.questions("Please type your passwords =>", function(paaword){
    if (password.length > 9)
    console.log("Success"){
    }else {
        console.log(no good)
    }



